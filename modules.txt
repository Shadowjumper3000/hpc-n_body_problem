#!/bin/bash
# Module configuration for N-Body simulation on HPC cluster

# Purge any existing modules to start clean
module purge

# Load required modules for CPU version
module load gcc/11.2.0
module load openmpi/4.1.1

# Optional: Uncomment if you need specific versions
# module load cmake/3.22.0

# Verify modules are loaded
echo "Loaded modules:"
module list
