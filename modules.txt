#!/bin/bash
# Module configuration for N-Body simulation on HPC cluster

# Note: Default modules are already loaded (gcc/12.3, openmpi/4.1.5)
# No need to reload them - just verify they're present

# Verify modules are loaded
echo "Loaded modules:"
module list
